package robLANG_ASOS.operations.data;

public class BreakData {
	
	public BreakData(Object object) {
		if(object != null && object instanceof BreakData) {
			BreakData other = ((BreakData) object);
		}
	}
	
	public BreakData() {}
}
