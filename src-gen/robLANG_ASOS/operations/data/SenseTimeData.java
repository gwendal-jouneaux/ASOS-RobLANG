package robLANG_ASOS.operations.data;

public class SenseTimeData {
	
	public SenseTimeData(Object object) {
		if(object != null && object instanceof SenseTimeData) {
			SenseTimeData other = ((SenseTimeData) object);
		}
	}
	
	public SenseTimeData() {}
}
